# 05 — Line 15: Baseline vs. Blocked/Special Variant (Control)

Method and headline finding: [docs/05_skip_comparison](../README.md).

**Revision round 3 (section B2): line 15 no longer appears in this
phase.** Its only "blocked" merged variant (count=4, one stop short of
the 48-stop reference) was recording noise, not a real detour (see
[docs/07's addendum](../../07_blockade_investigation/README.md) and the
main phase 05 README's count>5 fix). With zero blocks clearing the
significance floor, line 15 has no delta figure here.

**Answer:** no meaningful special-variant behavior exists for line 15 —
the apparent one in the first two revision rounds was entirely an
artifact of an over-permissive blocked-variant rule, not a real finding.

**Sources:** `govData/df_cleaned.csv`, `pipeline/plot_baseline_vs_blocked_delta.py`.
